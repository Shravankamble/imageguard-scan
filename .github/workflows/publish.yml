name: imageguard-scan testing composite action
on: 
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: Shravankamble/imageguard-scan@v0.1
      with:
        name: Shravan